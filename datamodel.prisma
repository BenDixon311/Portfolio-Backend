type Project {
  id: ID! @unique
  title: String!
  description: String!
  type: DevType!
  solo: Boolean! @default(value: "true")
  projects: [Project!]!
}

enum DevType {
  FRONT_END
  BACK_END
  FULL_STACK
  DESIGN
}

